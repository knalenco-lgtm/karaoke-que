# Kopieer dit bestand naar .env.local en vul je eigen waarden in.

# Upstash Redis (Vercel koppelt deze automatisch als je de database via
# Storage -> Create Database aanmaakt).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Pincode voor /host. Kies zelf iets, bijvoorbeeld 4821.
HOST_PIN=
